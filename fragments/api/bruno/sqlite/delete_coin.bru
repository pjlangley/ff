meta {
  name: delete coin
  type: http
}

delete {
  url: {{base_url}}/sqlite/coins/:ticker
}

params:path {
  ticker: PEPE
}

tests {
  test("204 no content", function() {
    expect(res.getStatus()).to.equal(204);
  });
}
