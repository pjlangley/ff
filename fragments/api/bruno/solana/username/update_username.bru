meta {
  name: update username
  type: http
  seq: 2
}

patch {
  url: {{base_url}}/solana/username/{{USERNAME_ADDRESS}}
}

headers {
  Content-Type: application/json
}

body {
  { "username": "bob" }
}

tests {
  test("200 OK and username updated", function() {
    const result = res.getBody();
    expect(res.getStatus()).to.equal(200);
  });
}
