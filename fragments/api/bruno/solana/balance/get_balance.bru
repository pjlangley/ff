meta {
  name: get address balance
  type: http
}

get {
  url: {{base_url}}/solana/balance/:address
}

params:path {
  address: 111111111111111111111111111111aa
}

headers {
  Content-Type: application/json
}

tests {
  test("200 OK and address balance returned", function() {
    const result = res.getBody();
    expect(res.getStatus()).to.equal(200);
    expect(Number(result.balance)).to.equal(0);
  });
}
