{
  "name": "ff-solana",
  "description": "slim version of the root package.json for `anchor test` in `docker.anchor.Dockerfile`",
  "version": "0.0.1",
  "author": "pjlangley",
  "license": "Unlicense",
  "engines": {
    "node": "^22",
    "npm": "^10"
  },
  "scripts": {
    "test": "tsx --test 'tests/*.test.ts'"
  },
  "devDependencies": {
    "@coral-xyz/anchor": "^0.30.1",
    "anchor-bankrun": "^0.5.0",
    "tsx": "^4.16.2"
  }
}
