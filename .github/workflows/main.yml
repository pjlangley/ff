on: [push]

jobs:
  node:
    runs-on: ubuntu-latest
    name: fullstack fragments - node.js
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: run all fragments
        uses: ./.github/actions/node
